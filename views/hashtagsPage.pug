extends layouts/main-layout.pug

block content 
    script. 
        var hashtag = '!{hashtag}';
        
    .resultsContainer 

    +createPostModals(userLoggedIn)
        
block scripts
    script(src="/js/hashtagsPage.js") 